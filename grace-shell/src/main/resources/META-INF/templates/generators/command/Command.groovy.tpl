package $packageName

import grails.cli.command.*

class ${className}Command implements ApplicationCommand {

    @Override
    boolean handle(ExecutionContext executionContext) {
        true
    }

}
